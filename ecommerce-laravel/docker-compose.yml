version: '3.8'

services:
  app:
    build: .
    ports:
      - "8000:80"
    volumes:
      - .:/var/www/html
    depends_on:
      - db

  db:
    image: postgres:15-alpine
    restart: always
    environment:
      POSTGRES_DB: php_user_signup
      POSTGRES_USER: render_user
      POSTGRES_PASSWORD: OutsqmszkxScLA2poJoVh0p7yg1aTbUS
    volumes:
      - db-data:/var/lib/postgresql/data

volumes:
  db-data: